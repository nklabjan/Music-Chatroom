import React, {Component} from 'react';
import Modal from 'react-bootstrap/Modal';

class AddSongModal extends Component {
    render() {
        return(
            <Modal>
                <Modal.Header>Add Song</Modal.Header>
            </Modal>
        )
    }
}

export default AddSongModal;